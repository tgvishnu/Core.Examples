@using System.Threading.Tasks
@{
    ViewData["Title"] = "Dependency Injection - Simple";
}
<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="panel panel-default">
        <div class="panel-heading">
            <h2 class="panel-title">MyDependency</h2>
        </div>
        <div class="panel-body">
            <p>The <code>MyDependencyController</code> has called the <code>IMyDependency</code> implemention (<code>MyDependencyService</code>). The service's <code>WriteMessage</code> method was called with a message string that indicates the controller called the method on the service. The <code>WriteMessage</code> method created a log entry with the message string.</p>
        </div>
    </div>
</div>